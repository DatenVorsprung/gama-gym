headless_dir = "/opt/gama-platform/headless"  # Path to the directory of gama headless folder
run_headless_script_path = headless_dir + "/gama-headless.sh"  # Path to the script that runs gama headless (gama-headless.sh)
